name: Deploy Cartalex App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: 🚀 Deploy on Server
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Checkout repository
        uses: actions/checkout@v4

      - name: 🚀 Connect to Server and Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          timeout: 30m
          script: |
            set -euxo pipefail

            echo "📂 Moving to project directory..."
            cd /home/cartalex-final/Cartalex

            echo "🔄 Fetching latest code..."
            git fetch origin main
            git reset --hard origin/main

            echo "🛑 Stopping old containers..."
            docker compose down

            echo "🏗️ Building and starting containers..."
            docker compose up -d --build

            echo "⏳ Waiting for services to be healthy..."
            until [ "$(docker inspect -f '{{.State.Health.Status}}' cartalex_db)" = "healthy" ]; do sleep 5; done
            until [ "$(docker inspect -f '{{.State.Health.Status}}' cartalex_pgtileserv)" = "healthy" ]; do sleep 5; done
            until [ "$(docker inspect -f '{{.State.Health.Status}}' cartalex_tegola)" = "healthy" ]; do sleep 5; done

            echo "✅ All services are healthy!"
            echo "🚀 Deployment completed successfully!"
